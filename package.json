{
  "scripts": {
    "test": "npm install && rm -rf cypress/reports && cypress run --config-file cypress.config.json --browser chrome --headed",
    "test-headless": "npm install && rm -rf cypress/reports && cypress run --config-file cypress.config.json --browser chrome"
  },
  "devDependencies": {
    "@cypress-audit/lighthouse": "^1.2.0",
    "@cypress/xvfb": "^1.2.4",
    "@shelex/cypress-allure-plugin": "^2.35.2",
    "cypress-mochawesome-reporter": "^3.3.0"
  },
  "dependencies": {
    "cypress": "^9.5.1",
    "cypress-audit": "^1.1.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.2.2",
    "mochawesome-report-generator": "^6.2.0",
    "@cypress/xvfb": "^1.2.4",
    "cypress-mochawesome-reporter": "^3.3.0"
  }
}
